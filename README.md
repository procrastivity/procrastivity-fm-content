# Procrastivity.fm Content Repo

This example shows the receiving side of the sync: a per-site content repository populated by the reconciler.

The workflow in `.github/workflows/trigger-pages-build.yml` triggers a Cloudflare Pages deploy hook whenever `main` changes.
